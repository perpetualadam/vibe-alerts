/**
 * Shared marketing / billing CTA copy.
 *
 * Prefer outcome-focused labels over generic "Start free trial" / "Subscribe".
 * Trial availability still drives which CTA we show; the label sells the benefit.
 */

/** Primary signup / trial CTA — pain-aware, product-specific. */
export const CTA_TRIAL = 'Stop Losing Leads';

/** Paid checkout when no free trial is configured. */
export const CTA_SUBSCRIBE = 'Unlock Pro Features';
